# venvs
.venv/
venv/

# python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# generated dataset (rebuild with: python -m src.data_prep)
data/processed/

# model output dirs / checkpoints (weights live on HF Hub, not git)
byt5-sanskrit-ocr/
*.safetensors
*.bin
checkpoint-*/

# HF cache
.cache/

# eval raw json + transient logs (kept as summaries in docs/ instead)
eval/results/*.json
eval/results/*.log
# ...but keep the LoRA-vs-full result (deterministic; the chart + README cite it)
!eval/results/lora_vs_full.json

# throwaway models written by the LoRA-vs-full experiment (full ckpt ~1 GB)
eval/compare_runs/

# scratch/throwaway helper scripts (underscore-prefixed)
scripts/_*.py

# internal dev notes (kept local; not part of the public/submitted repo)
CLAUDE.md
docs/SUBMISSION_EMAIL.md

# ImmverseAI assignment source materials — local reference only, NEVER publish
_assignment_source/

# OS
.DS_Store
Thumbs.db

# notebooks runtime
.ipynb_checkpoints/
