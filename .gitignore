# venvs
.venv/
venv/

# python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# generated dataset (rebuild with: python -m src.data_prep)
data/processed/

# model output dirs / checkpoints (weights live on HF Hub, not git)
byt5-sanskrit-ocr/
*.safetensors
*.bin
checkpoint-*/

# HF cache
.cache/

# eval raw json (kept as summaries in docs/ instead)
eval/results/*.json

# internal dev notes (kept local; not part of the public/submitted repo)
CLAUDE.md

# OS
.DS_Store
Thumbs.db

# notebooks runtime
.ipynb_checkpoints/
